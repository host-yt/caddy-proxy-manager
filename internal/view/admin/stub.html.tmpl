{{define "stub"}}
<div class="r-card text-center" style="background:rgb(var(--surface));color:rgb(var(--text))">
  <div class="t-small" style="color:rgb(var(--muted))">This page is part of the next MVP iteration.</div>
  <div class="t-caption mt-1" style="color:rgb(var(--muted))">Path: <code>{{.Path}}</code></div>
</div>
{{end}}
